# Issue Worker

Render‑hosted worker that parses links and generates PDFs.

## Required Env
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `WORKER_SHARED_SECRET`
- `RESEND_API_KEY` (optional)
- `APP_BASE_URL` (required for email links)

## Start
- `npm install`
- `npm start`
